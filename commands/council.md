---
description: Convene the Council of Five — independent parallel advisors who pressure-test a decision instead of agreeing with you. Balanced mode.
---

Invoke the `council` skill with balanced mode (all 5 advisors equal weight).

The user's question is below. If no question was given, ask for one in a single short sentence — do not lecture about how the skill works.

**Question:** $ARGUMENTS
