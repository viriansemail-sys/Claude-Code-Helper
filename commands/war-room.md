---
description: Council variant for the "how do we ship this fast?" question — Executor-led, ends in a plan with dates. Use when the strategic decision is settled and the real question is execution.
---

Invoke the `council` skill in **war-room** mode. Read `~/.claude/skills/council/variants/war-room.md` and follow that variant's synthesis structure (Executor-led, ends in a dated plan).

The user's question is below. If no question was given, ask for one in a single short sentence.

**Question:** $ARGUMENTS
